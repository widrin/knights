syntax = "proto3";

package centerrpc;
option go_package = "../centerrpc";
// 通用的，没有额外数据的rep返回
message  MessageBody {
  bytes Data =1;
}
service CenterService {
  rpc Request(MessageBody) returns (MessageBody); //请求消息
  rpc RequestIn(MessageBody) returns (MessageBody); //内部请求消息
}
